var inc = 0.01

function setup() {
    createCanvas(200, 200);
    pixelDensity(1);
}

function draw() {
    var y_offset = 0;
    loadPixels();
    for (var y=0; y < height; y++) {
        var x_offset = 0;
        for (var x=0; x < width; x++) {
            var index = x + y*width;
        }
    }
}